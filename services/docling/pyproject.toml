[project]
name = "rag-experiment-docling-serve"
version = "0.1.0"
description = "Isolated Docling Serve runtime for rag-experiment"
requires-python = ">=3.10"
dependencies = [
    "docling-serve[ui]>=1.20.0,<2",
]

