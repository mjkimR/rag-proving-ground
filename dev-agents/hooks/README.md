# Dev Hooks

Hooks executed at key stages of the development cycle (e.g., pre-commit, post-run verification) to automate agent-time quality checks.

> [!NOTE]
> These hooks run during local development (like Git hooks or harness interceptors) to ensure code quality and prevent instruction drift.
